# dbt Cloud Explorer coordinates used to build the semantic-definition links
# referenced from charts/semantic_links.yml and charts/dashboard.yml.
#
# Fill these in from dbt Cloud: Deploy > Environments > (your environment) —
# the account_id, project_id, and environment_id are all in that URL.
#
# Explorer resource URL shape:
#   https://cloud.getdbt.com/deploy/{account_id}/projects/{project_id}/environments/{environment_id}/details/{unique_id}
# where {unique_id} is one of:
#   model.stephans_snow_sandbox.<model_name>
#   metric.stephans_snow_sandbox.<metric_name>

account_id: "<TODO>"
project_id: "<TODO>"
environment_id: "<TODO>"
